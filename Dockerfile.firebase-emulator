FROM node:22-alpine

# Install Java 21 (required by Firebase emulators) and Firebase CLI
RUN apk add --no-cache openjdk21-jre \
  && npm i -g firebase-tools

WORKDIR /app

# Optional: expose default emulator ports
EXPOSE 8085 4000
